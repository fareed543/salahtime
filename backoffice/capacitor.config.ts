import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wallet.oneportal',
  appName: 'One Portal',
  webDir: 'dist/oneportal'
};

export default config;
